<Window x:Class="PR25._01_CustomerList.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PR25._01_CustomerList"
        xmlns:view="clr-namespace:PR25._01_CustomerList.View"
        xmlns:viewmodel="clr-namespace:PR25._01_CustomerList.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <viewmodel:MainWindowViewModel></viewmodel:MainWindowViewModel> 
    </Window.DataContext>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <ListView Grid.Column="0" Margin="30" ItemsSource="{Binding Path=Customers}" SelectedItem="{Binding SelectedCustomer}"></ListView>
        <view:CustomerEditView Grid.Column="1" DataContext="{Binding SelectedCustomer}"></view:CustomerEditView>
    </Grid>
</Window>
