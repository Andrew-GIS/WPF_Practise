<Window x:Class="PR_SelfCustomer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PR_SelfCustomer"
        xmlns:ViewModel="clr-namespace:PR_SelfCustomer.CustomerViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="400" Width="600">
    <Window.DataContext>
        <ViewModel:CustomerEditViewModel></ViewModel:CustomerEditViewModel>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="0.5*"></RowDefinition>
        </Grid.RowDefinitions>

        <DataGrid Grid.Row="0" Grid.ColumnSpan="2" Margin="20" AutoGenerateColumns="True" ItemsSource="{Binding Customers}"></DataGrid>

        <Button Grid.Column="0" Grid.Row="1" MaxWidth="80" MaxHeight="40" Content="GET" Command="{Binding GetCommand}"></Button>
        
    </Grid>
</Window>
